using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollectableBehavious : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // world spawn

        // collection reaction

        // delayed respawn
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
