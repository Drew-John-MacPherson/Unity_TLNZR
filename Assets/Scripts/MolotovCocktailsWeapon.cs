using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MolotovCocktailsWeapon : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Particle emisions

        // Audio

        // Actor damage 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
