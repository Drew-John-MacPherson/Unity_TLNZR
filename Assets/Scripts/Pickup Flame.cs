using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickupFlame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // distinct audio
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
