using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickupMolo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Distinct audio
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
